This directory contains UI components migrated from `frontend/components/` (Next.js) into Vite+React.
We keep the same component implementations, only adjusting import paths and any Next-specific APIs.



